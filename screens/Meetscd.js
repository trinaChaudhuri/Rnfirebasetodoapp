import React from 'react';
import { Text, View } from 'react-native';

const Meetscd = ({navigation}) => {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>
        Meeting scheduler page! 🎉
      </Text>
    </View>
  );
}

export default Meetscd;